{
  "$comment": "Model matrix. Each entry runs every sample. url/apiKey may be OMITTED — then they fall back to env: IMPRESSION_EVAL_URL and (apiKeyEnv | IMPRESSION_EVAL_KEY | OPENAI_API_KEY). variant picks distiller prompt; maxTokens feeds the length-note math.",
  "configs": [
    {
      "name": "default-env",
      "model": "claude-opus-4-8",
      "variant": "third-person",
      "maxTokens": 8192
    }
  ]
}

---
id: incremental-not-restate-overview
type: judge
applies_to: compress
---
This is a follow-up RE-READ. The history ALREADY has the overview: rules are a trigger/predicate/
enforce triple with the four enforcement actions. The user explicitly asked the SHARPER question
("exact full shape, is it just a flat triple or is there recursion/nesting, show the complete
grammar, where composition happens — don't repeat the overview") — this is an INCREMENTAL read,
equivalent to a recall: surface only what's NEW.
Score 5 if the note delivers the NEW shape facts: the rule HEADER is a flat triple, but recursion
lives in TWO inner slots — (1) predicate composes via not/and/or/parens, (2) enforcement chains
via ';' (sequence); and atom is the non-recursive leaf. It should NOT re-explain the basic
trigger/predicate/enforce triple or re-list the four enforcement actions (that's the overview
already in history). Score 1-2 if it restates the overview (re-defines the triple / re-lists the
four actions) instead of extracting the grammar/recursion answer — restating history is the
failure this guards against.

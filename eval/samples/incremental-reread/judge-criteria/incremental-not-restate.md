---
id: incremental-not-restate
type: judge
applies_to: compress
---
This is a RE-READ. The history already contains a BROAD overview note (title, authors, SMU,
"framework/DSL", the contributions list, 95.56% precision). The user explicitly asked for the
SPECIFIC rule mechanism now, "not the overview again". Score INCREMENTAL EXTRACTION 1-5:
- 5 = the note surfaces the NEW concern-specific detail from THIS section — the
  trigger / predicate / enforcement triple as the rule shape, the kinds of enforcement
  (e.g. user confirmation / retrospective self-examination), the intervention being at
  runtime per agent action — and does NOT re-state the broad overview (authors, SMU, the
  95.56% number, the generic "it's a framework") already in history.
- 2 or below = it re-emits the overview already in history (authors/affiliation/precision/
  generic framing) instead of extracting the rule mechanism, OR adds nothing new beyond the
  prior note. Re-stating what history already has is the failure this guards against
  (the RELEVANCE rule: compress the NEW result, don't restate the conversation).

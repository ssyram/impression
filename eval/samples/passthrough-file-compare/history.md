### user
The staging and prod configs drifted. Read prod's config.yaml — I'm going to compare it line-by-line against staging's, so I need the EXACT contents, every key and value, no summarizing.
### assistant
Reading prod config.yaml verbatim for the line-by-line comparison.

### user
I only need to change how the retry DELAY is computed. Where exactly is the backoff delay calculated? Ignore everything else about the retry system.
### assistant
Grepping the retry module for the delay/backoff computation.

---
id: passthrough-justified
type: judge
applies_to: passthrough
---
The agent will paste this license clause WORD-FOR-WORD into a legal notice, "character-exact,
every comma and capitalization matters", explicitly "don't summarize". Every clause is directly
relevant and none can be safely summarized — the exact phrasing IS the deliverable. This is a
verbatim need that does NOT neatly match the 5 example cases (it's not a skill/playbook, not a
file-diff, not a re-run) — it tests that the single principle (all relevant + needs exact
phrasing) works for novel cases, not just the listed examples.
Score 5 if the distiller PASSED THROUGH. Score 1-2 if it summarized the clause (losing the
exact legal wording = direct execution failure).

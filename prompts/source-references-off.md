## Current-result source references disabled

This current tool result has no source-reference index. No reference expansion or literal unescaping occurs: `@!…@` is not expanded and literal escape sequences such as `@|!` are not decoded. Emit a self-contained note using ordinary text. Existing exactness, repeated-read, sentinel, and passthrough rules remain unchanged.
